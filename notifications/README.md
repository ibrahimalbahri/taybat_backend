# Notifications App

Purpose
- Store device tokens for push notifications.

Key Models
- DeviceToken

Main APIs
- Register device: `POST /api/notifications/device`

Notes
- Token registration uses upsert by token.
